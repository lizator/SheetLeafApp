<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dk.rbyte.sheetleafapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SheetLeafApp"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".inside.gamesOverview.create.CreateCharacterActivity"
            android:exported="true" />
        <activity
            android:name=".inside.NavigationActivity"
            android:exported="true" />
        <activity
            android:name=".outside.CreateProfileActivity"
            android:exported="true"
            android:windowSoftInputMode="stateVisible" />
        <activity
            android:name=".outside.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>